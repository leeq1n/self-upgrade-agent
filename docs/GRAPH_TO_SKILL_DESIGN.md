# Graph → flat converter (per user 2026-07-14 final insight)

> L0: Revised architecture — SUA graph + on-demand
> converter + flat skill export.  Per user
> clarification 2026-07-14: "我需要的是一个图谱→
> 平铺式的转换工具, 这样就可以随时导出一个skill,
> 给到其他项目使用".
> Last P20-verified: 2026-07-14 (initial)

## The revised architecture (per user)

Per user 2026-07-14 final insight: instead of
**manually maintaining** 2 projects (SUA graph +
skill-v2 flat), use a **converter tool** that
**on-demand** exports the SUA graph as a flat
skill for any target project.

### Before (per c53) — manual 2-project

```
hermes-root/
├── self-upgrade-agent/   # graph (manually maintained)
└── agent-onboarding-v2/  # flat (manually synced)
```

Problem: manual sync = drift + extra work.

### After (per this commit) — graph + converter

```
hermes-root/
├── self-upgrade-agent/
│   ├── docs/             # graph (single source of truth)
│   └── tools/
│       └── graph_to_skill.py   # NEW: converter
│
└── (NO skill-v2 project)  # skill is generated output
```

Benefit: **single source of truth** (SUA graph) +
**on-demand generation** (per project, per export).

## How the converter works

### Input

- SUA docs (graph view):
  - `docs/PRINCIPLES.md` + `PRINCIPLES_DETAIL.md`
  - `docs/OPERATING_RULES.md` + `OPERATING_RULES_DETAIL.md`
  - `docs/MCP_TOOLS.md`
  - `docs/RECURSIVE_DECOMPOSITION.md` + others

### Output

A skill package (per Hermes skill format):
- `SKILL.md` (entry point, ≤ 100 lines, L0 layer)
- `references/` (L1 + L2 detail, sequential)
- `assets/` (templates, examples — if needed)
- `scripts/` (helper scripts — if needed)

### Generation flow

```
$ python .hermes/scripts/graph_to_skill.py \\
    --source self-upgrade-agent/docs/ \\
    --target ./exported-skill/ \\
    --name "agent-onboarding" \\
    --description "..." \\
    --tags "..."

# Reads SUA graph, traverses cross-refs,
# produces SKILL.md + references/*.md
```

## Why this is better than 2-project (per c53)

| Aspect | 2-project (c53) | Graph + converter (this) |
|---|---|---|
| Source of truth | 2 copies (graph + flat) | 1 (graph) |
| Sync effort | manual, ongoing | none (generated) |
| Drift risk | high (2 copies diverge) | low (single source) |
| Exportability | per project (rewrite) | per project (one command) |
| Maintenance | high (update both) | low (update graph only) |
| Per P7 奥卡姆 | violates (redundant) | satisfies (no redundancy) |
| Per P11 摘要+引用 | violates (must sync) | satisfies (auto-generated) |
| Per P13 no orphan | satisfies (manual refs) | satisfies (auto-generated refs) |

**Decision per "选哪个最优" (P7 奥卡姆)**: graph +
converter is optimal per P7 + P11 + P13.

## Per P22 stuck→plan + P25 6-step

This is a **plan revision** triggered by user insight.
Per P25 step 5 (impact analysis): commits 54-57 in
c53 plan are now superseded by this revised plan.

**Revised plan (commits 54-58)**:

| # | Commit | Content |
|---|---|---|
| **54** | this commit | Design doc + revised plan |
| **55** | `feat(.hermes/scripts): graph_to_skill.py converter (MVP)` | Converter implementation |
| **56** | `feat(converter): test export with first skill (agent-onboarding)` | Test run |
| **57** | `docs(EXTENSIONS): update X2 = converter output, not Hermes global` | Cross-ref |
| **58** | parent verification | SUMMARY_LIFECYCLE |

**C53 plan (54-57) is now obsolete**:
- 54 (skill-v2 skeleton) — N/A (generated output)
- 55 (SKILL.md + references) — generated by commit 56
- 56 (EXTENSIONS.md X2 update) — still relevant
  (commit 57 in revised plan)
- 57 (SUA → skill cross-ref) — N/A (graph is
  source of truth, no cross-ref needed)




## Per M-self-application 4-level

- **Level 1**: ✅ 2 files (1 new + 1 updated) + 1
  commit.
- **Level 2 (rule itself)**: P7 奥卡姆 + P11 + P13
  + P20 + P22 + P25 + P26 all applied.  7 rules.
- **Level 3 (memory / project structure)**: SUA
  graph + converter architecture replaces manual
  2-project sync.  Plan file updated.
- **Level 4 (own operating behavior)**: future
  knowledge additions go to graph (SUA); skill
  export is automated (no manual sync).

## Detail (L2)

For per-principle analysis, M-self-application, follow-ups, and other L2 detail, see [`GRAPH_TO_SKILL_DESIGN_DETAIL.md`](GRAPH_TO_SKILL_DESIGN_DETAIL.md).  Per R6, this companion is required for files > 7KB.
