# SKILL_DESIGN — Detail (L2)

> L0: L2 detail for `SKILL_DESIGN.md`.  Per P11
> 摘要+引用, the summary file is the L0/L1 layer
> (≤ 7KB); this file is the L2 layer (extended
> guidance).  Per R6, this companion is referenced
> from the summary.

---

### 核心 area 1: 4 sub-knowledge areas (above, "What 'skill design' means")

**For skill projects**: read first, understand
the 4 areas (What / How / Preserve / Verify)
before incubating.

### 核心 area 2: 5-phase process (above, "Skill design as a 5-phase process")

**For skill projects**: follow Observe → Decide →
Spawn → Codify → Maintain.  Each phase has
specific output format.

### 核心 area 3: 5 self-preservation rules (above, "What 'skill preservation' means")

**For skill projects**: MUST obey these 5 rules.
Violating any = broken skill.

### 核心 area 4: 4 conditions to incubate (above, "When to incubate")

**For skill projects**: 4-condition strict AND
must all be true.  Otherwise don't incubate.

### 核心 constraint: SUA's M-n 11 (sub-project) + M-n 13 (layer-extension) + M-n 14 (two-track reasoning)

These M-rules (in `docs/OPERATING_RULES.md`) govern
how skill-incubator should self-organize.  Skill
projects should know these M-rules apply to their
incubation process.

### How to use this 核心 marker

**For skill projects reading skill-incubator**:

1. Read this 核心 段 first (above).
2. Then read the 4 核心 areas in order.
3. Skip README.md / HANDOFF.md / case-studies.md
   (not core).
4. Cross-reference SUA's OPERATING_RULES.md for
   M-rules that govern incubation.

This marker is the **entry point** for skill
projects (per 信息拓扑 方案 C, c81).

## Changelog (per M-n 12 + M-n 13 self-application)

- c88: initial SKILL_DESIGN.md (canonical, from SUA
  c83 deprecation)
- c102: add 核心 内容 marker段
  (per user message 2026-07-15)
- 2026-07-20: consolidated back to SUA from
  skill-incubator (per project re-architecture,
  skill-incubator now archived).